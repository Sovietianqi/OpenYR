#pragma once

// Combat/ParticleTypeClass.h - compatibility redirect.
// ParticleTypeClass is defined in Particles/ParticleTypeClass.h. This header
// exists for legacy include paths (e.g. <Combat/ParticleTypeClass.h>) and
// simply re-exports the real definition without duplicating the class.
#include "../Particles/ParticleTypeClass.h"
